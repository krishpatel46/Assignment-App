<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AssignmentApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- NOTE: Since NO Light theme colors were provided, same colors from the Dark theme are used -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorTertiary">@color/green</item>
        <item name="colorSecondary">@color/off_white</item>
        <item name="colorSurfaceContainerLow">@color/off_black</item>
        <item name="colorSurfaceContainerLowest">@color/mid_black</item>
        <item name="colorOutline">@color/grey</item>
        <item name="colorOnSurface">@color/light_grey</item>
    </style>

    <style name="Theme.AssignmentApp" parent="Base.Theme.AssignmentApp" />
</resources>